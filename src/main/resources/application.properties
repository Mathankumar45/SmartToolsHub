spring.application.name=SmartToolsHub

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/SmartToolsHub
spring.datasource.username=root
spring.datasource.password=Mathan@12
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8080

# Allow large uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# JPA/Hibernate Configuration
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.com.SmartToolsHub=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mathanpravee368@gmail.com
spring.mail.password=xpyu tubv ayhy cgqf

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

admin.email=mathanpravee368@gmail.com

# Railway provides the full connection URL in an environment variable
# We just tell Spring Boot to use it.
spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver

# IMPORTANT: Tell Hibernate to use the PostgreSQL language
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate Settings (these can stay the same)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Show more security logs
logging.level.org.springframework.security.web.FilterChainProxy=DEBUG

# Disable CORS in Spring Security (we'll handle it manually)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=false